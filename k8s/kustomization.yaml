apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: crud-veiculos

resources:
  - namespace.yaml
  - configmap.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - hpa.yaml

commonLabels:
  app: crud-veiculos
  version: v1.0.0
  managed-by: kustomize
